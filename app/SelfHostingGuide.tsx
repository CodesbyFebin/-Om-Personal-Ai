"use client";
import Link from "next/link";
import { useState } from "react";
const steps=[
 ["prerequisites","Prerequisites","Ubuntu 24.04 LTS or maintained Linux, Git 2+, Node.js 22+, npm 10+, Docker Engine 26+ with Compose v2. Start with 4 CPU cores, 16 GB RAM and 40 GB free storage.","node --version\nnpm --version\ndocker compose version\ngit --version"],
 ["clone","1. Clone OM","Clone your OM repository on the owned server. Replace the URL only if your fork uses another name.","git clone https://github.com/CodesbyFebin/-Om-Personal-Ai.git\ncd -Om-Personal-Ai"],
 ["install","2. Install and validate","Install the lockfile exactly and run all project checks. Never deploy dependencies from an unreviewed branch.","npm ci\nnpm run lint\nnpm run build"],
 ["environment","3. Configure private environment","Keep secrets outside Git. Generate unique credentials, restrict file permissions, and never publish administrative ports.","cp .env.example .env\nchmod 600 .env\nopenssl rand -base64 48"],
 ["services","4. Start sovereign services","Deploy Ollama, Qdrant and MinIO on a private Docker network. Pin versions and keep volumes on encrypted disks.","docker compose pull\ndocker compose up -d ollama qdrant minio\ndocker compose ps"],
 ["models","5. Install a local model","Pull a model that fits your hardware. OM stays Offline until an authenticated probe succeeds.","docker compose exec ollama ollama pull qwen2.5-coder:7b\ncurl --fail http://127.0.0.1:11434/api/tags"],
 ["database","6. Initialize durable data","Hosted OM uses D1. A standalone distribution must apply its checked-in migration; browser storage is never the source of truth.","npm run db:generate\n# inspect generated SQL\nnpm run build"],
 ["production","7. Run production","Start OM behind HTTPS. Bind the application and services to loopback or a private network, with rate limits and secure headers.","npm run build\nnpm start\ncurl --fail http://127.0.0.1:3000/api/health"],
 ["connect","8. Connect services","In Sovereign Setup, verify each owned endpoint. A service becomes Online only after a real probe returns evidence.","Ollama   http://127.0.0.1:11434\nQdrant   http://127.0.0.1:6333\nMinIO    http://127.0.0.1:9000\nEdge     http://127.0.0.1:8787"],
 ["hardening","9. Harden the host","Use firewall allowlists, rootless containers, encrypted backups, credential rotation, non-root accounts and fail-closed networking.","sudo ufw default deny incoming\nsudo ufw allow 22/tcp\nsudo ufw allow 443/tcp\nsudo ufw enable"],
 ["verify","10. Final verification","Test authentication, owner isolation, audit events, restore, degraded states, connector timeouts and approval boundaries.","curl --fail https://your-om-domain.example/api/health\ndocker compose ps\ndocker compose logs --tail=100"]
];
const envVars=[["OLLAMA_URL","Local inference endpoint","http://127.0.0.1:11434","Connector"],["POSTGRES_URL","Standalone relational database","postgresql://…","Standalone"],["REDIS_URL","Queues, leases and cache","redis://127.0.0.1:6379","Runtime"],["ANVIL_RPC_URL","Local development chain","http://127.0.0.1:8545","Connector"],["QDRANT_URL","Sovereign vector memory","http://127.0.0.1:6333","Connector"]];
export default function SelfHostingGuide(){const[copied,setCopied]=useState("");const copy=async(id:string,value:string)=>{await navigator.clipboard.writeText(value);setCopied(id);setTimeout(()=>setCopied(""),1400)};return <main className="guide-shell"><aside className="guide-toc"><Link href="/">ॐ <b>OM</b></Link><small>SELF-HOST GUIDE</small><nav>{steps.map(x=><a href={`#${x[0]}`} key={x[0]}>{x[1]}</a>)}</nav><Link className="guide-back" href="/sovereign-setup">Open Sovereign Setup →</Link></aside><article className="guide-main"><header><p>OWN YOUR INTELLIGENCE</p><h1>Complete self-hosting guide</h1><span>Install OM’s private runtime on infrastructure you control, connect every service through measured probes, and keep credentials away from the browser.</span><div><b>10 verified stages</b><b>Ubuntu + Docker</b><b>Local-first</b><b>Zero false status</b></div></header><section className="guide-alert"><strong>Architecture note</strong><p>The live OM workspace uses owner authentication and a D1-backed API. The pasted guide’s Express/SQLite, public signup and NotebookLM API claims are excluded until a standalone backend implements and verifies them.</p></section><section id="environment-variables" className="env-guide"><div className="env-head"><div><small>SERVER-ONLY CONFIGURATION</small><h2>Environment variables</h2><p>Document keys in .env.example, keep values in an ignored .env locally, and use your production secret manager for deployment. Connector endpoints are validated when used—not during the web build—so an offline owned service cannot break deployment.</p></div><button onClick={() => copy("env", envVars.map(item => `${item[0]}=`).join("\n"))}>{copied === "env" ? "Copied ✓" : "Copy template"}</button></div><div className="env-table">{envVars.map(v => <article key={v[0]}><code>{v[0]}</code><span>{v[1]}</span><em>{v[2]}</em><b>{v[3]}</b></article>)}</div><div className="env-rules"><strong>Safe runtime policy</strong><ul><li>Never expose secrets through NEXT_PUBLIC_ variables or browser storage.</li><li>Do not log connection strings, tokens or credentials.</li><li>Validate URL protocol and host before server-side requests to prevent SSRF.</li><li>Hosted OM uses managed D1; POSTGRES_URL applies only to a verified standalone distribution.</li><li>Vercel, Netlify and Docker values must be entered directly in their secret controls—not committed.</li></ul></div></section>{steps.map((x,i)=><section id={x[0]} className="guide-step" key={x[0]}><div className="guide-number">{String(i+1).padStart(2,"0")}</div><div><h2>{x[1]}</h2><p>{x[2]}</p><pre><code>{x[3]}</code><button onClick={()=>copy(x[0],x[3])}>{copied===x[0]?"Copied ✓":"Copy"}</button></pre></div></section>)}<section className="guide-checks"><h2>Release checklist</h2>{["HTTPS and secure cookies verified","No public database or model ports","Unique secrets stored outside Git","Backups restored in a test environment","Owner isolation and audit trail tested","Offline services remain visibly offline","Edge commands require explicit approval","Containers pinned and scanned"].map(x=><label key={x}><input type="checkbox"/> {x}</label>)}</section></article></main>}
